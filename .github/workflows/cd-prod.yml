name: Deploy Prod

on:
    push:
        branches: [ "main" ]
    workflow_dispatch:

permissions:
    contents: read
    id-token: write

jobs:
    deploy:
        uses: ./.github/workflows/_deploy.yml
        with:
            service: draw-my-today
            database_id: draw-my-today-db
            profile: production
            environment: prod
            image_tag: latest
        secrets: inherit
